# DESIGN.md — つなげて消す デザイン正本

このゲームの「見た目の正解」を記録した正本。新しい画面・要素を作るときは、ここの値とルールを再利用して**雰囲気をブレさせない**こと。
※ 単一 `index.html` プロジェクトなので、トークンは `<style>` 内の CSS 変数とこの表が二重の正本。

## デザインの方向性（ひとことで）

ダークネイビーの宇宙トーン × キャンディのようにぷっくり丸い高彩度パネル × ネオンのグロー。
スマホ前提・高コントラストの白文字・大きめタップ領域。「静かで上質、でも遊び心」。

## カラートークン（`:root`）

| 変数 | 値 | 用途 |
|---|---|---|
| `--bg` | `#0f1226` | 背景ベース（濃紺） |
| `--panel` | `#1b1f3b` | パネル/カードの暗部 |
| `--accent` | `#5ad1ff` | 主役のシアン（ロゴ・選択・ライン） |
| `--good` | `#46e6a0` | 成功/達成（緑） |
| `--bad` | `#ff6b8b` | 失敗/警告（赤ピンク） |
| `--gold` | `#ffd75e` | ★スタート・迎撃モードの金 |
| `--text` | `#eef1ff` | 文字（ほぼ白） |
| `--muted` | `#9aa0c9` | 補助テキスト（くすみ青紫） |

背景: `radial-gradient(120% 120% at 50% 0%, #1a1f44 0%, var(--bg) 55%)`
フォント: `-apple-system,BlinkMacSystemFont,"Hiragino Sans","Helvetica Neue",sans-serif`

## 数字パネルの9色（`data-v="1〜9"` → `--c1`(上)/`--c2`(下) の縦グラデ）

| 数字 | --c1 | --c2 | 色味 |
|---|---|---|---|
| 1 | `#e05c5c` | `#b03838` | 赤 |
| 2 | `#e07a30` | `#b05520` | 橙 |
| 3 | `#d4b428` | `#a08010` | 黄 |
| 4 | `#5cb85c` | `#3a8a3a` | 緑 |
| 5 | `#2ab8a0` | `#1a8870` | 青緑(teal) |
| 6 | `#3ab4e0` | `#2080b0` | 水色 |
| 7 | `#5a6ee0` | `#3848b0` | 藍 |
| 8 | `#9a50d0` | `#6a2ea0` | 紫 |
| 9 | `#d050a0` | `#a02878` | マゼンタ |

数字なしのデフォルトパネル: `linear-gradient(180deg,#3a4080,#2a2f5e)`

## 形状・質感

- **角丸**: パネル `14px` / 盤・カード `22px` / チップ・ボタン `12〜18px`
- **盤面**: `gap:8px`, `padding:8px`, 背景 `rgba(255,255,255,.04)`, 枠 `1px rgba(255,255,255,.06)`
- **パネルの立体感（重要）**: `box-shadow:inset 0 -3px 0 rgba(0,0,0,.25), 0 2px 6px rgba(0,0,0,.3)` …底に暗い内側影でぷっくり感
- **半透明サーフェス**: 補助UIは白の `rgba(255,255,255,.04〜.08)` で重ねる（ガラス質）
- **レイアウト幅**: コンテンツは `min(94vw,440px)`、要素間 `gap:12px`
- 文字は基本 `font-weight:800〜900`、数字は `clamp(20px,7vw,34px)`

## 状態表現

- **★スタート**: 金の内枠 `inset 0 0 0 3px var(--gold)` ＋ グロー、左上に小さな `★`
- **選択中(sel)**: `linear-gradient(180deg,#6fd6ff,#3aa8e6)` ＋ `scale(.9)` ＋ 白内枠＋シアングロー
- **達成可/ぴったり(match)**: 緑 `#73f3b6→#33c889`（合計OK・最適の合図）
- **超過だが撃墜可(okover)**: アンバー `#ffd27a→#e6a23a`（迎撃モードで敵を超えた＝倒せるが減点。sum文字は `#ffc14d`、ライン `#ffb84d`）
- **超過/失敗(over)**: 赤 `#ff8aa3→#e6536f`（パズル＝「超えたら無効」／迎撃＝敵+2を超える「オーバーで外れる」）
- **消去(clear)**: `scale(0)` にポップ
- 経路ライン(SVG): 通常=accent / match=`#46e6a0` / over=`#ff6b8b`

## 迎撃モードの敵チップ（脅威で色が変わる）

- 遠い（安全）: `#5a9ee0→#2858a0`（青）
- 近い（step≤3）: `#ffa53a→#c06a10`（橙）
- 直前（step≤1 / danger）: `#ff5a6e→#c02a3c`（赤・点滅）
- 標的ロック: 白 `outline:3px` ＋ 白グロー

## モーション

`drop`(落下) / `pop`(消去) / `toast`(中央演出) / `cardin`(結果カード) / `logoIn`(タイトル) / `demobob`(デモパネル上下) / `shine`(ボタンの光沢) / `enemyDanger`(敵の点滅) / `shatterFall`(迎撃の撃墜＝敵色の破片`.shard`が弾けて落下＆フェード)。
イージングは概ね `cubic-bezier(.2,.9,.3,1.12)` 系の弾む動き。

## やらないこと（トーンを壊さないために）

- フラットな原色ベタ塗り（必ず縦グラデ＋底の内側影でぷっくりさせる）
- 直角・細枠の事務的UI（角丸と余白でやわらかく）
- 多色の乱用（アクセントはシアン、特別＝金、成功＝緑、失敗＝赤、の4軸に寄せる）
