# 引き継ぎメモ（HANDOFF）

> ## ⚠️ 2026-06-11：リポジトリ移管＋履歴リセット済み（モバイルは再clone必須）
> 1. **リポは `takeshi-hariu/number-link-puzzle` に移管済み**（旧 haristweet は削除・コラボからも除外）。公開URLも **https://takeshi-hariu.github.io/number-link-puzzle/** に変わった（旧URLは404）。
> 2. **git履歴を1コミットに作り直して force-push 済み**（haristweet表記の除去のため）。**古いクローンでは `git pull` が失敗する**ので、モバイル側は**フォルダごと削除して再clone**すること：
>    `git clone https://github.com/takeshi-hariu/number-link-puzzle.git`
> 3. コミットIDは `user.name=takeshi-hariu` / `user.email=takeshi-hariu@coly.info` に統一。
> 4. **push が `denied to haristweet` (403) で弾かれる場合**：ghのアクティブ垢が haristweet に戻っている。`gh auth switch -u takeshi-hariu` で直る（恒久対策は `gh auth logout -u haristweet` で旧垢を外す）。

別環境（モバイルアプリ等）で続きをやるための引き継ぎ。**まず `git pull` で最新の `main` を取得してから作業すること**（↑初回は再clone）。

## このリポジトリは何か
スマホ向けゲーム「つなげて消す」＋その派生プロトタイプ群。公開先 GitHub Pages：
- 本体: https://takeshi-hariu.github.io/number-link-puzzle/
- 試作: `/ten.html`（10つなぎ連鎖）, `/poker.html`（つなげてポーカー対戦）

## ファイル構成（2026-06 メインを差し替え）
- `index.html` … **メイン＝「つなげてポーカー」対戦版**（旧 poker.html を昇格）。隣接5枚で役・CPU対戦・段位制・斜めボーナス。←ここを育てる。
- `archive/classic.html` … 旧本体「つなげて消す」v1.6（3モード）。**消さずに隠してある**。見た目の正本は DESIGN.md。
- `archive/ten.html` … 旧試作「キリ番つなぎ連鎖」。
- `DESIGN.md` … 旧本体(classic)の見た目の正本。配色トーンの参考に。
- `README.md` … ポーカー説明＋アーカイブ＋旧Changelog。

## いまの検討の流れ（重要な経緯）
1. 本体は完成したが「なんか面白くない」となった。
2. 診断：コアが「お題の数字を探すだけ」で、**戦略・偶然性・創発が無い**。味付け（モード/SE/演出）を足しても床は平ら。
3. ユーザーの定義：**「パズルの面白さ＝自分で“仕込んだ”上での偶然の快感」**（ぷよ/テトリスの大連鎖型）。
4. `ten.html`（10つなぎ連鎖）を試作 → 連鎖は出るが「**ランダム補充の連鎖＝自分の手柄じゃない**」のがUX的にイマイチ、と判明。根因＝ランダム補充が計画を殺す。
5. 方針転換アイデア＝**パネルをトランプにしてポーカー**。役ランクがあるので「妥協 vs 粘って上位役」という**能動的な仕込み**がルールに内蔵される。
6. `poker.html` 試作：隣接5枚をつないで役→ワンペア以上で消去・得点。「意外と面白い」。
7. さらに **CPUとの対戦形式**（同じ盤面で交互）に進化させた＝現在の `poker.html`。

## 2026-06 追記：メイン=ポーカー対戦。タイトルにメニュー（ストーリー/フリー対戦）＋ストーリーモード追加
- ストーリー＝会話劇→ポーカー対戦(vsキャラ5人)→会話劇。`CHARS`/`SCRIPT`(index.html内)で差し替え可。Storyエンジン＋#story吹き出しUI。
- バトルは既存マッチを流用（`startMatch`）、`endGame`が `mode==='story'` で `storyEndGame()` に分岐（勝→つづく→`Story.battleResult(true)`、負→リトライ）。対戦相手は段位連動の難易度＋名前表示。
- **要バランス確認**：テストAI(斜め未使用・最善役)でも最弱「見習い」に150-350で負けた。見習いがやや強すぎるかも。

## キャラ立ち絵（v1.1〜）
- ストーリーの会話はVN風立ち絵レイアウト。`assets/characters/<key>.png`(透過PNG, 3:4) を置けば立ち絵表示、無ければ絵文字フォールバック。keyは CHARS のキー（dealer/rookie/hana/jin/carmen/king/you）。
- 生成プロンプト・スペックは `assets/characters/PROMPTS.md`。画像は未生成（ユーザーがアプリ等で作って入れる予定）。

## ★ メイン：マジポカ（旧マージ＆昇天）（現在 v2.6＝`index.html`）

> **改称（v2.6）**：タイトルを「マージ＆昇天」→**「マジポカ」**（MERGE×POKER）に変更。タイトルロゴ/タブ名/ヘッダー表示のみ。v2.6.1でクリアトーストも「昇 天 ！」→「WELL DONE!」（プレイヤー画面から「昇天」表記を除去）。
> **v2.7＝★クリアの格ゲー風シネマ演出**：黒帯レターボックス＋盤面モノクロ化(`.wrap.cine-dim`)＋特大「WELL DONE!」スラム＋勝利5枚をカラーのまま文字前面にクローン表示(`#cine`/`cineFx`/`cineEnd`、winStage冒頭で1.5秒→従来の⭐へ)。
> **v2.7.1＝CHAIN表示のドッジ**：なぞり中の指が上2段ならHUDを盤面下端へ(`#chainHud.low`)、下2段で上端復帰、中段据え置き（`hudDodge`、start/moveで発火、チェイン終了でリセット）。
> **v2.8＝ローレゾ（8bit風）モード**：タイトルの「ローレゾ」ボタンで `html.lofi` 全切替（`localStorage 'majipoka_lofi'`・起動時復元）。スキャンライン(`#scanlines`)＋カクカク札＋フラット配色＋ドット字（DotGothic16をON時のみ遅延読込・`loadLofiFont`）。コード＝`applyLofi`/`initLofi`、見た目のみ・挙動無変更。**v2.8.1でPC-88寄せ**＝デジタル8色（盤面=PC-88ブルー#0000a0＋シアン枠、原色レッド札、緑/黄）＋太い走査線＋オーバースキャン黒枠。調整は `html.lofi` ブロック内の色／`#scanlines` の濃さ。
> **v2.9＝ドパミン演出（全力）**：マージ/チェイン/天井札でパーティクル大量噴射＋フラッシュ＋画面パンチ＋縁ティント。チェイン段で青天井にエスカレーション（×5で爆発）。コード＝`burst`/`popText`/`flashScreen`/`tintEdge`/`screenPunch`/`chainJuice`（WAAPIで描画）＋要素 `#fx`/`#jflash`/`#jtint`。`armFreeze(cx,cy)`に座標追加、`doMerge`が噴射起点を渡す、`topCardFx`はジャックポット噴射。強度つまみ＝`chainJuice`内の係数。挙動は無変更。
> **v2.10＝★クリアの紙吹雪**：`confetti(count)` を新設、`winStage` で発火。背面レイヤー `#confetti`（z-index:-1＝カードの裏／盤パネルが半透明なので隙間から透ける）に色付き紙片をWAAPIで落下。要素 `#confetti`／CSS `.conf`。
> **v2.12＝バージョン表記を一元管理**：タイトル画面とゲーム画面下の2か所に手書きしていたバージョン表記を、JS先頭の `const VERSION` 1か所に集約（`#verTitle`/`#verGame` に流し込み）。**今後の改版は `VERSION` の値だけ更新すればよい**（HTML側の手書き修正は不要）。

> **現状（2026-06-10）**：マージ＆昇天が**本体に昇格**＝`index.html`化（旧ポーカー対戦は `archive/poker.html` へ／取り残しの旧 `merge.html` も `archive/merge.html` へ退避）。**宿題だった「3回以内にK→A(★昇天)へ届くか」は達成済み**（v1.9時点で実機全クリ確認＝難易度カーブは当たり）。以降は体験の磨き込みフェーズ。
> - **モバイル更新 v2.0〜2.2**：NEXT先読み3枚／UI刷新「読む→見る」(初回チュートリアル・目標カード絵・役プレビューを変化後の絵・上部バー簡素化)／タイトル画面内蔵。
> - **v2.3＝ワールド・ポーカー・ツアー（入国スタンプ収集）**：全クリ＝世界のギャンブル都市1つ制覇→パスポートに入国スタンプ。格は**コンティニュー使用数**で決定（0回=金🥇/1回=銀🥈/2回以上=銅🥉）。タイトルに**パスポート画面**（全12都市 LAS→…→MCM・`localStorage 'merge_passport_v1'` 永続・全踏破後は格上げ）。コード＝`CITIES`/`awardStamp`/`showStamp`/`renderPassport`/`openPassport`、全クリ分岐は `winStage` 内。
> - **v2.4＝マージチェイン**：役成立でタイマーが `CHAIN_FREEZE`(=1.0)秒ストップ。その1秒で次のなぞりを“始め”れば停止（ドラッグ中/演出中は減らない）、役成立で CHAIN++＆再フリーズ。外す/時間切れで連鎖終了→再開、連鎖中は鼓動も停止。`armFreeze`/`freezeTick`/`endFreeze`/`resetChain`、`doMerge`末尾発火。**v2.4.1：完全停止だと簡単すぎたのでA案＝スロー化**（連鎖中は止めず `CHAIN_SLOW`(=0.25)倍速で流す。interval `timeLeft-=0.2*(frozen?CHAIN_SLOW:1)`）。つまみ＝`CHAIN_FREEZE`/`CHAIN_SLOW`/`STAGES[].time`。
> - **v2.5＝NEXT先読み廃止**：チェインの時間圧で「見る間がない」ため、NEXT表示＋先読みキュー(nextQueue/renderNext/resetQueue)を削除。補充は `genCard` その場呼び出しに統一＝低札寄せロジック・挙動は無変更（`refillCard`は`genCard`の別名に縮小）。
> - **次の候補**：①パスポートを“地図”表示に（世界地図＋フライト弧）＝今はグリッド ②全踏破/全金の特別フィナーレ ③都市ごとの味付け ④チェイン連長ボーナス（育ち+1）/金条件に最大チェイン絡め ⑤ペア乱発が出れば連鎖起点をツーペア以上に絞る。

**塩漬けにしかけた merge を「天井＝目標」を入れて作り替えたら一気に化けた → 本採用→本体昇格。** ユーザー評価「めちゃおもしろい」。
- **何が効いたか**：マージ自体は悪くなかった、**ゴールが無かっただけ**。各ステージに**天井＝目標カード**を置いて「天井を5枚つないで★＝クリア」にした瞬間、“仕込み”が生まれた。
- **仕様（現状）**：
  - ステージ毎に天井（目標カード）：St1=6 → 7 → … → St9=A（激ムズ）。`STAGES` 配列で定義。
  - **補充は 2〜(天井-1) を低札寄り。天井札は湧かない＝自力で作る。**
  - 役→1枚に集約、天井で頭打ち。**強い役ほど一気に天井へ**（ストレート/フラッシュが近道）。目標未満の5カードは天井札を1枚生成。
  - **目標カードを5枚つなぐ（ファイブカード）＝★昇天＝ステージクリア。** 制限時間切れでリトライ。
  - 進捗ピップ（★n/5）・天井札の金枠グロー・クリア/失敗オーバーレイ。
- **ポーカーが効くようになった**：制限時間下では「ペアで+1」より「ストレ/フラで一気に天井」が得＝**役の使い分けに意味が出た**（ずっと死んでた問題がここで解決）。
- **次の作業＝難易度カーブの調整**：序盤ぬるめは“教習”として意図的にOK。ただし**後半でちゃんと牙を剥くか**が肝。現状 `time` が後半ほど増える（90→A:160）ので**中盤が間延びしてぬるいかも**。ユーザーがSt3〜4を踏んだ体感（「このへんから歯ごたえ欲しい」）を聞いて、中盤の時間を締める／必要なら「盤面フル＝失敗」を足す。
- 調整レバー＝`STAGES[].time`（各面の制限時間）／`refillCard()`の低札寄せ具合／`mergeResult`の`BUMP`（役ごとの上がり幅）。
- **難易度カーブ（v1.8.3）**：実機1時間プレイで「Q（St7）で物量がしんどく断念、K/A＝★昇天に未到達」と判明。対策＝`refillCard()`の下限を `max(2, top-7)` に引き上げ（高ステージほど2,3が出ない＝“1つ下のティアから天井を組む”）。これでAまで現実的に到達可能化。窓幅の定数「7」が効きどころ（小さく＝より易しく/大きく＝据え置き寄り）。
- **危険演出（v1.8〜v1.8.2）**：残り60秒から背景が10秒ごとに赤く濃く（`#dangerWash`）、残り30秒から鼓動（`#dangerPulse`／`beatLoop`で視覚パルスと`navigator.vibrate`を同期・加速）、節目で赤フラッシュ（`#dangerFlash`）＋「のこり」チップ赤光＋数字tpop、ラスト5秒は毎秒tick＋画面シェイク。※**iOS Safariはvibrate非対応**（iPhoneでは振動しない）。
- **コンティニュー制（v1.9）**：1ラン3回、タイムアップで同ステージ再挑戦（クリア後も繰り越し）、使い切ると次の失敗でゲームオーバー→`startPlay()`。残量●表示（`#conts`）。下部ボタンは「はじめから」＝`startPlay()`（ラン全体リセット）。`showOverlay`は2ボタン対応。回数は`CONTINUE_MAX`。
- **演出（v1.7・実装済み）**：集約の吸い込み→ポップ（`gatherAnim`/`.card.merged`）。天井札ミント時に特別演出（`topCardFx`：金リング`.ring`／ソフト金フラ`#flash.soft`／進捗ピップpop／`navigator.vibrate`／`Snd.top(level)`で**音程が1段ずつ上昇**＝★への“ため”）。★勝利時はバイブのクレッシェンド。※常時の脈打ちグローは「複数並ぶとうるさい＋落下/ポップ animation と干渉」のため見送り、天井札は静止の強グローのまま（必要なら子要素オーバーレイで後付け可）。

### 棚上げ中のアイデア：役を“技”にするポーカー・デュエル
- 「役の種類ごとに効果（攻撃/防御/妨害/奪取）を変える」対戦案。マージ＆昇天が“役の使い分け”問題を別解で解決したので**当面は棚上げ**。対戦をさらに深めたくなったら復活候補（A=HP制 / B=役別効果 / C=盤面操作）。

## TODO（あとで必ずやる・ユーザー希望／条件は未確定）
- **ふってシャッフルを“最強アイテム”として序盤は封印 → 進行で解放**：いきなり使えるのをやめ、ラウンド/対戦をクリアしていくと解放する形にしたい。解放条件は未確定（候補：N勝ごとにストック／段位で解放／ストーリー中盤で習得イベント）。現状はゲーム開始時から使える。
- **ふってシャッフル後の相手リアクションをキャラ別口調に**：今は共通7種ランダム。`CHARS`に各キャラの reaction を持たせ、ストーリー戦ではそのキャラの声で出し分け（味付け、後で必ずやる）。

## メイン(index.html)の現在地（＝続きはここから）
- 仕様：あなた→CPU交互、各 **6ラウンド**、隣接5枚で役、合計点が高い方が勝ち。
- CPU AI：盤面から **常に最高役を取る greedy**（`findBestHand`）。→ **強すぎ**（検証で初手フルハウス80点）。
- 役と得点：ペア10／ツーペア20／スリーカード30／ストレート50／フラッシュ60／フルハウス80／フォーカード120／ストフラ250／ロイヤル500。

## 次の候補（未着手・ユーザーと相談しながら）
- **CPU難易度**（よわい／ふつう／つよい。例：上位N役からランダム、たまにミス）← まず最優先になりそう
- お題役/ボーナス、3本勝負、CPUの「ためる」駆け引き演出、ローカル2人対戦
- 盤面の圧（捌けないと詰む）

## 運用ルール（ユーザー合意・必ず守る）
- **機能追加ごとにメイン(index.html=ポーカー)のバージョンを繰り上げ**（新機能=マイナー、小修正/バランス=パッチ）。**現在 v1.0**。表示はタイトルメニュー下の `.menu-version`。README「更新履歴(Changelog)」に毎回追記。
  - （旧本体 classic.html は v1.6 で凍結。アーカイブなので以後バージョンは進めない）
- **更新履歴を README の「更新履歴(Changelog)」に毎回追記**。
- 見た目を変える時は **DESIGN.md** を尊重（ダーク×高彩度キャンディ×ネオン。アクセント=シアン/金/緑/赤）。
- コミット：`user.name=takeshi-hariu` / `user.email=takeshi-hariu@coly.info`、`Co-Authored-By: Claude` 付き、push 先は `main`。
- GitHub Pages は main 直 push で数十秒後に自動反映。

## 注意
- 本体(index.html)とポーカー試作は別物。**面白い方を本命に育てる**段階。決定前に本体は壊さない。
- 環境を行き来する時は**作業前に必ず pull、作業後に push**。両方で同じファイルを同時編集しない。
